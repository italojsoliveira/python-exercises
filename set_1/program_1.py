# a program that reads the user name as an input and outputs a welcome message

user_name = input('Write your first name: ')

print('Welcome,', user_name, '!')

print('Welcome, {}!'.format(user_name))